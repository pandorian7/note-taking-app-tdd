meta {
  name: Missing Invalid Body
  type: http
  seq: 2
}

post {
  url: http://localhost:8081/notes
  body: json
  auth: inherit
}

tests {
  test("should return 400 for invalid body", function () {
    const status = res.getStatus();
    const body = res.getBody();
  
    expect(status).to.equal(400);
    expect(body.code).to.equal("INVALID_BODY");
  
  });
  
}

settings {
  encodeUrl: true
}
