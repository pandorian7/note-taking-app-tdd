meta {
  name: Missing Content
  type: http
  seq: 4
}

post {
  url: http://localhost:8081/notes
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Note Title"
    
  }
}

tests {
  test("should return 400 for missing content", function () {
    const status = res.getStatus();
    const body = res.getBody();
  
    expect(status).to.equal(400);
    expect(body.code).to.equal("REQUIRED_ARGUMENT_MISSING");
    expect(body.field).to.equal("content");
  });
  
}

settings {
  encodeUrl: true
}
